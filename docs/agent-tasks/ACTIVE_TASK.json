{
  "id": "DSH-VISION-RC1-WEBSERVER-LIFECYCLE-009",
  "mode": "IMPLEMENT",
  "source_branch": "main",
  "source_commit": "LATEST",
  "objective": "Fix the current source-built dsh-plugin-image-mind 0.2.x web route lifecycle so `/image-mind/*` is registered on the real @deepseek-ai/dsh@0.1.1-rc.1 webServer during normal `dsh web` boot, then prove the fix against the actual isolated rc.1 listener. Keep the real local Harness read-only and keep dsh-vision/image-mind uninstalled there.",
  "context": "008 proved exact rc.1 composes and boots the current source-built 0.2.0 packages and that the route handler works when an in-process Context is explicitly given a webServer, but the normal rc.1 `dsh web` listener returns GET /image-mind/catalog and /image-mind/previews -> 404 and POST /image-mind/ -> 405. Current src/index.ts registers systemPrompt via Cordis lifecycle injection but discovers webServer through ctx.get() plus a bounded setTimeout poll. Treat the 008 evidence as a lifecycle-registration defect until disproven. Prefer the Cordis service lifecycle seam (`ctx.inject(['webServer'], ...)`) over polling; preserve immediate registration when webServer already exists, handle delayed availability, and avoid duplicate registration on the same webServer instance. Do not change route behavior, trust policy, attachment ownership, provider logic, package versions, or Harness state. If evidence shows a different smallest root cause, document it and keep the fix narrowly scoped.",
  "allowed_changes": [
    "packages/image-mind/src/index.ts",
    "packages/image-mind/src/runtime/webserver-lifecycle.ts",
    "packages/image-mind/tests/webserver-lifecycle.test.ts",
    "packages/image-mind/tests/integration.test.ts",
    "docs/agent-results/DSH-VISION-RC1-WEBSERVER-LIFECYCLE-009-result.json"
  ],
  "forbidden_changes": [
    "packages/vision/**",
    "packages/image-mind/src/attachments/**",
    "packages/image-mind/src/client/**",
    "packages/image-mind/src/adapters/**",
    "packages/image-mind/src/tools/**",
    "scripts/**",
    ".github/**",
    "README.md",
    "CHANGELOG.md",
    "docs/compatibility.md",
    "package.json",
    "package-lock.json",
    "workspace package manifests",
    "manual or direct edits to any real DSH_HOME file or directory",
    "manual or direct edits to any real DeepSeek Harness profile, cordis.patch.yml, manifest, lockfile, settings file, credential file, cache, node_modules tree, plugin tree, or other Harness-owned state",
    "any real Harness plugin add/install/reinstall/remove action for dsh-vision/image-mind",
    "package publishing, npm dist-tags, tags, releases, or version bumps",
    "writes to Ran-sh/dsh-crew or any repository other than Ran-sh/dsh-vision",
    "credentials, API keys, secret values, signed URL secrets, attachment ids, private local paths, or unredacted secret-bearing logs",
    "unrelated files"
  ],
  "validation": [
    "node .agent-workflow/validator/validate-contract.mjs task docs/agent-tasks/ACTIVE_TASK.json",
    "record exact source commit, OS, Node, npm, pnpm, and exact @deepseek-ai/dsh@0.1.1-rc.1 versions",
    "read-only verify at task start that the real local Harness does not actively compose dsh-plugin-image-mind or @ran-sh/dsh-vision; do not modify real Harness state",
    "add focused regression coverage proving route registration when webServer is already available and when webServer becomes available after image-mind apply; prove the same webServer is not registered twice",
    "remove the bounded setTimeout polling ownership from image-mind route registration and use the Cordis webServer service lifecycle seam, unless a smaller proven lifecycle fix is required by the real rc.1 API",
    "npm run typecheck",
    "npm run build",
    "npm test",
    "npm run test:package",
    "npm run test:built",
    "create a task-owned temporary launcher/cache/DSH_HOME/source-package staging outside the repository and load artifacts built from the exact source commit into exact @deepseek-ai/dsh@0.1.1-rc.1 without using the real Harness state",
    "boot exact rc.1 with normal `dsh web` in the isolated environment and prove the actual HTTP listener serves GET /image-mind/catalog with HTTP 200 from the current source-built plugin",
    "prove at least one additional /image-mind route reaches the plugin handler on the real listener (for example a trusted-local config/previews request or an intentionally invalid POST yielding the plugin's own 4xx envelope rather than host 404/405)",
    "prove the isolated composition is current source-built 0.2.0 and not the published 0.1.x line",
    "verify no duplicate-prefix registration or duplicate handler invocation occurs on one server lifecycle",
    "delete all task-owned temporary launcher/cache/DSH_HOME/package/probe artifacts and terminate all task-owned processes",
    "final read-only verification that the real local Harness still does not actively compose dsh-plugin-image-mind/@ran-sh/dsh-vision and was not modified by this task"
  ],
  "acceptance_criteria": [
    "Task Contract validates before execution and the exact source SHA is recorded.",
    "Implementation stays only within the allowed image-mind lifecycle/test files; no attachment/client/provider/tool semantics are changed.",
    "The production route-registration path no longer depends on a bounded timer poll to discover webServer.",
    "Focused tests prove immediate and delayed webServer availability and no duplicate registration for the same server lifecycle.",
    "Repository typecheck, build, tests, package integrity, and built-artifact checks PASS; failures must be reported truthfully and must not be hidden.",
    "PASS for the rc.1 runtime fix requires the actual isolated `dsh web` listener to return HTTP 200 for GET /image-mind/catalog and to route at least one additional /image-mind request into the plugin handler. In-process-only evidence is insufficient.",
    "The Result must distinguish current-source rc.1 runtime evidence from registry compatibility; 0.2.0 remains unpublished and this task must not publish it.",
    "No real Harness lifecycle operation is authorized; the user's real local Harness remains read-only and dsh-vision/image-mind remains uninstalled after completion.",
    "All task-owned temporary state and processes are removed after evidence capture.",
    "Result Contract v2 includes second-precision timezone-aware timeline.started_at/timeline.completed_at and validator-owned PASS evidence from `node .agent-workflow/validator/validate-contract.mjs result docs/agent-results/DSH-VISION-RC1-WEBSERVER-LIFECYCLE-009-result.json --stamp`.",
    "On completion, delete docs/agent-tasks/ACTIVE_TASK.json and commit/push only allowed implementation/test files, the Result Contract, and ACTIVE task deletion on the executor branch."
  ],
  "result_contract": "docs/agent-results/DSH-VISION-RC1-WEBSERVER-LIFECYCLE-009-result.json",
  "completion_commit_contract": [
    "packages/image-mind/src/index.ts",
    "packages/image-mind/src/runtime/webserver-lifecycle.ts",
    "packages/image-mind/tests/webserver-lifecycle.test.ts",
    "packages/image-mind/tests/integration.test.ts",
    "docs/agent-results/DSH-VISION-RC1-WEBSERVER-LIFECYCLE-009-result.json",
    "docs/agent-tasks/ACTIVE_TASK.json"
  ],
  "delete_active_task_on_completion": true,
  "metadata": {
    "executor": "CODEX",
    "purpose": "fix-and-prove-rc1-webserver-route-lifecycle",
    "related_issue": "21",
    "compatibility_target": "@deepseek-ai/dsh@0.1.1-rc.1",
    "source_line": "current-0.2.x-unpublished",
    "real_install_policy": "remain-uninstalled-until-final-approved-version",
    "supersedes_blocker_from_task": "DSH-VISION-RC1-BROWSER-REGRESSION-008",
    "prepared_from_commit": "33aeb08d52ca524d83d48a2da69e4e824f62141a"
  }
}
