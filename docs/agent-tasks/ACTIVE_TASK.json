{
  "id": "DSH-VISION-0201-REMEDIATION-CANDIDATE-022",
  "mode": "IMPLEMENT",
  "source_branch": "main",
  "source_commit": "LATEST",
  "objective": "Prepare the exact 0.2.1 remediation candidate after the defective public 0.2.0 npm artifacts: bump both publishable packages to 0.2.1, update the image-mind dependency to @ran-sh/dsh-vision@^0.2.1, and add a package-level fail-safe so a clean checkout cannot `npm publish`/`npm pack` an empty shell when lib/** has not already been built. Prove the complete built tarballs and all release gates on Linux/Windows/macOS as applicable. Do NOT perform npm registry writes in this preparation stage; leave the Draft PR unmerged for ChatGPT review. The user has already authorized the later exact 0.2.1 npm publication and deprecation of both broken 0.2.0 versions after this candidate is merged and publicly verified.",
  "context": "Batch 021 proved both public 0.2.0 tarballs are defective empty shells because the maintainer manually published from a fresh checkout without build output: @ran-sh/dsh-vision@0.2.0 contains only package.json + README.md, while dsh-plugin-image-mind@0.2.0 lacks lib/index.js/lib/client.js/lib/cli.js and therefore has no working npx bin. Repository code and CI remain green. npm package versions cannot be reused after publication, so remediation must use 0.2.1. The prior accepted 0.2.0 product candidate already passed exact @deepseek-ai/dsh@0.1.1-rc.2 internal/browser/npx packed-artifact regression; no unrelated feature change is desired. This stage exists to make the 0.2.1 repository candidate safe and mergeable before any new registry write.",
  "allowed_changes": [
    "packages/vision/package.json",
    "packages/image-mind/package.json",
    "package-lock.json",
    "package.json",
    "packages/vision/build.mjs",
    "packages/image-mind/build.mjs",
    "scripts/**",
    "tests/**",
    "packages/vision/tests/**",
    "packages/image-mind/tests/**",
    ".github/workflows/ci.yml",
    "README.md",
    "CHANGELOG.md",
    "packages/image-mind/README.md",
    "docs/release-checklist.md",
    "docs/compatibility.md",
    "docs/verification-debt.md",
    "docs/agent-results/DSH-VISION-0201-REMEDIATION-CANDIDATE-022-result.json"
  ],
  "forbidden_changes": [
    "npm publish, npm unpublish, npm deprecate, npm dist-tag add/rm, package visibility/access mutation, provenance mutation, token creation, or any other npm registry write in this preparation stage",
    "Git tag creation/deletion/push, GitHub Release creation/editing/deletion, or release asset upload",
    "installing/enabling the plugin in the user's real Harness or reading/modifying real Harness profiles/settings/credentials/node_modules/store/cache",
    "changing the release target away from exactly 0.2.1 for either publishable package",
    "changing exact @deepseek-ai/dsh family compatibility away from the already accepted 0.1.1-rc.2 line unless a concrete regression proves it necessary",
    "unrelated product feature work, provider behavior changes, benchmark changes, hosted-provider credential use, force-push or history rewrite",
    "any release guard that silently packages stale lib/** output; the guard must either freshly build the package during npm packaging/publishing or fail closed before an incomplete tarball could be produced"
  ],
  "validation": [
    "validate this Task Contract; fetch latest main and record the exact source SHA; create one task branch and one Draft PR early; do not force-push",
    "change packages/vision version from 0.2.0 to exactly 0.2.1 and packages/image-mind version from 0.2.0 to exactly 0.2.1; change image-mind runtime dependency from @ran-sh/dsh-vision@^0.2.0 to exactly the intended compatible range @ran-sh/dsh-vision@^0.2.1; update lock metadata consistently without unrelated dependency churn",
    "inspect npm lifecycle behavior used by the installed npm version and implement the smallest robust package-level release safety mechanism for BOTH publishable packages so `npm pack`/`npm publish` from a normal fresh checkout after dependency install cannot ship manifests without required lib/** artifacts. Prefer a standard npm lifecycle hook such as prepack that performs the package build or an equally fail-closed mechanism; do not rely on a human remembering `npm run build`",
    "add regression coverage for the exact incident: start from a task-owned/repository test state where publishable lib/** outputs are absent, invoke the actual npm packaging lifecycle, and prove the resulting tarballs contain working built entries rather than metadata-only shells. If the chosen guard intentionally fails instead of building, prove it fails before package publication with a clear error. The final public workflow should be simple and safe from a fresh clone after npm ci",
    "run npm ci, npm run typecheck, npm run build, npm test, npm run test:package, npm run test:built and git diff --check; require all green",
    "for packages/vision, create/inspect a real npm pack artifact (not source-only inference) and require version 0.2.1 plus lib/index.js and expected lib/types declarations, with no source/tests/private credential material. Record file count, unpacked size, shasum/integrity if available",
    "for packages/image-mind, create/inspect a real npm pack artifact and require version 0.2.1 plus lib/index.js, lib/client.js, lib/cli.js, source maps expected by the package files policy, cordis.patch.yml and README; verify bin dsh-plugin-image-mind -> lib/cli.js and runtime dependency @ran-sh/dsh-vision@^0.2.1; exclude source/tests/private credential material. Record file count, unpacked size, shasum/integrity if available",
    "run side-effect-free npm publish --dry-run --access public for vision then image-mind from the final candidate and require both to include the full built file sets. Explicitly compare against the defective 0.2.0 public state (2 files vision / 3 files image-mind) so a metadata-only regression cannot pass",
    "verify read-only registry state before completion: both 0.2.1 exact versions should still be absent; both broken 0.2.0 versions should still exist and latest may still point at them. Perform no mutation in this stage",
    "rerun the packed-artifact npx lifecycle acceptance against exact @deepseek-ai/dsh@0.1.1-rc.2 using the 0.2.1 local packed artifacts, including install, status, status --json, same-version update, composition/boot smoke as existing matrix allows, uninstall, absent status and idempotent second uninstall. Preserve Linux/Windows/macOS lifecycle coverage on final PR head",
    "update release documentation only as needed to state that 0.2.0 on npm is defective and must not be used, 0.2.1 is the remediation candidate, and publication must use the package lifecycle guard / verified packed artifacts. Do not claim 0.2.1 published before it actually is",
    "final PR head CI must be green for the repository's existing Node 22/24, Windows, install-roundtrip, secret-scan and all cli-lifecycle lanes; if workflow changes are unnecessary, preserve existing matrix",
    "write docs/agent-results/DSH-VISION-0201-REMEDIATION-CANDIDATE-022-result.json with Result Contract v2, exact source/result commits, second-precision timezone-aware timestamps, version/lock changes, release-guard design, exact incident regression evidence, final pack contents, dry-run output summary, registry read-only state and final CI run",
    "delete docs/agent-tasks/ACTIVE_TASK.json on the execution branch after completion, validate/stamp the Result Contract, commit/push, leave the Draft PR unmerged and stop"
  ],
  "acceptance_criteria": [
    "Both publishable manifests are exactly version 0.2.1 and image-mind depends on @ran-sh/dsh-vision@^0.2.1 with consistent lock metadata.",
    "A clean checkout after npm ci cannot reproduce the 0.2.0 empty-shell incident: actual npm packaging either freshly builds required lib/** artifacts automatically or fails closed before an incomplete tarball is produced.",
    "Actual 0.2.1 pack and publish-dry-run artifacts contain the complete required built files, including vision lib/index.js/types and image-mind lib/index.js/lib/client.js/lib/cli.js with a valid bin mapping.",
    "Full repository/package/built gates and packed-artifact npx lifecycle regression against exact DSH 0.1.1-rc.2 are green, including final PR-head CI platform lanes.",
    "No npm registry write, Git tag, GitHub Release or real-Harness touch occurs in this stage; public 0.2.1 remains absent until the merged-main publication stage.",
    "Result Contract v2 validates with --stamp, blockers are empty and status is PASS only if the 0.2.1 candidate is safe to publish from merged main; ACTIVE_TASK is deleted on the execution branch and the Draft PR remains unmerged for ChatGPT review."
  ],
  "result_contract": "docs/agent-results/DSH-VISION-0201-REMEDIATION-CANDIDATE-022-result.json",
  "completion_commit_contract": [
    "docs/agent-results/DSH-VISION-0201-REMEDIATION-CANDIDATE-022-result.json",
    "docs/agent-tasks/ACTIVE_TASK.json"
  ],
  "delete_active_task_on_completion": true,
  "metadata": {
    "executor": "CODEX",
    "purpose": "prepare-safe-0.2.1-remediation-candidate-after-defective-0.2.0",
    "related_issue": "21",
    "target_version": "0.2.1",
    "broken_public_version": "0.2.0",
    "harness_target": "@deepseek-ai/dsh@0.1.1-rc.2",
    "user_authorized_0_2_1_publish": true,
    "user_authorized_0_2_0_deprecate": true,
    "registry_writes_allowed_in_this_stage": false,
    "publish_after_merged_main": true,
    "deprecate_only_after_0_2_1_public_verification": true,
    "tag_release_authorized": false,
    "real_harness_install_authorized": false
  }
}
