{
  "id": "DSH-VISION-RC1-ISOLATED-COMPAT-006",
  "mode": "TEST_ONLY",
  "source_branch": "main",
  "source_commit": "LATEST",
  "objective": "Uninstall the currently installed local dsh-vision plugin through the official DeepSeek Harness plugin manager, leave the real local Harness without dsh-vision for the remainder of optimization, and validate published dsh-plugin-image-mind@0.2.0 against exact @deepseek-ai/dsh@0.1.1-rc.1 only in a fully isolated temporary Harness environment. Do not reinstall dsh-vision into the real local Harness at the end of this task.",
  "context": "The user explicitly authorizes one real-environment lifecycle action at the start of this task: remove the currently installed dsh-vision/image-mind plugin through the official Harness plugin mechanism. This authorization does NOT permit direct edits to DSH_HOME, profiles, cordis.patch.yml, manifests, lockfiles, settings, credentials, caches, or any other Harness-owned file. Project code/scripts remain forbidden from mutating Harness state. After the official uninstall, the real local Harness must remain without dsh-vision until a future explicit final-install task is created after the best version has been selected. All rc.1 compatibility mutation/testing in this task must use newly created temporary DSH_HOME and launcher/cache directories. Issue #13 proved that the previous `npx @deepseek-ai/dsh ...` startup stall occurs in npm Arborist transient dependency resolution before DSH starts, while the same exact rc.1 package starts successfully once pre-resolved with pnpm. Issue #21 tracks rc.1 as the compatibility target and the deferred real-provider checks. Do not change docs/compatibility.md; ChatGPT will update compatibility status after reviewing the Result Contract.",
  "allowed_changes": [
    "docs/agent-results/DSH-VISION-RC1-ISOLATED-COMPAT-006-result.json"
  ],
  "forbidden_changes": [
    "packages/**",
    "scripts/**",
    "tests/**",
    ".github/**",
    "README.md",
    "CHANGELOG.md",
    "docs/compatibility.md",
    "package.json",
    "package-lock.json",
    "manual or direct edits to any real DSH_HOME file or directory",
    "manual or direct edits to any real DeepSeek Harness profile, cordis.patch.yml, manifest, lockfile, settings file, credential file, cache, plugin tree, or other Harness-owned state",
    "any real Harness plugin add/install/reinstall action for dsh-vision/image-mind after the initial explicitly authorized uninstall",
    "writes to Ran-sh/dsh-crew or any repository other than Ran-sh/dsh-vision",
    "credentials, API keys, secret values, or unredacted secret-bearing logs",
    "unrelated files"
  ],
  "validation": [
    "node .agent-workflow/validator/validate-contract.mjs task docs/agent-tasks/ACTIVE_TASK.json",
    "node --version && npm --version && pnpm --version",
    "read-only inspect the real local web profile/plugin roster to determine whether dsh-plugin-image-mind / @ran-sh/dsh-vision is currently installed and record only safe package/version identifiers",
    "if installed, use the already-available Harness CLI or another locally pre-resolved official Harness CLI to run the official plugin-manager remove operation for dsh-plugin-image-mind from the real web profile; do not use a fresh transient npx resolution path merely to perform the uninstall",
    "after the official uninstall, prove by read-only inspection that the real local Harness no longer composes dsh-plugin-image-mind or @ran-sh/dsh-vision, and prove the Harness itself can still reach its normal web readiness without the plugin",
    "npm run typecheck",
    "npm run build",
    "npm test",
    "npm run test:package",
    "npm run test:built",
    "create fresh temporary launcher, npm/pnpm cache, and DSH_HOME directories; install exact @deepseek-ai/dsh@0.1.1-rc.1 into the launcher with pnpm so the known transient npx resolver stall is not part of the compatibility verdict",
    "using only the temporary DSH_HOME, run the exact rc.1 DSH binary through the official Harness plugin command to add published dsh-plugin-image-mind@0.2.0 to a temporary web profile",
    "start exact rc.1 web against the temporary DSH_HOME on a dedicated localhost port; prove readiness with an HTTP 200/listening signal and then terminate only the task-owned process tree",
    "inspect the isolated composed plugin/config output and runtime/tool registry to prove both @ran-sh/dsh-vision and dsh-plugin-image-mind are loaded and understand_image is registered",
    "when a usable existing provider credential or keyless endpoint can be consumed without copying/writing real Harness settings, run one bounded real image understanding call using a task-owned fixture and record only redacted provider/model/route/trace evidence; otherwise record the exact credential/provider blocker without changing any real settings",
    "using only the temporary DSH_HOME, run the official Harness plugin remove command for dsh-plugin-image-mind@0.2.0 and prove the plugin/bundle layers are absent afterward",
    "delete all task-owned temporary launcher/cache/DSH_HOME directories after evidence is captured",
    "final read-only check: the real local Harness still has dsh-vision/image-mind uninstalled; do not reinstall it"
  ],
  "acceptance_criteria": [
    "The Task Contract validates before execution and the exact source SHA used is recorded in the Result Contract.",
    "At task start, determine whether the real local dsh-vision/image-mind plugin is installed. If present, remove it only through the official DeepSeek Harness plugin manager. If already absent, record that fact and do not fabricate an uninstall.",
    "The explicitly authorized real-environment mutation is limited to that official uninstall operation. No manual/direct edit of Harness-owned files is allowed before, during, or after it.",
    "Record the exact official uninstall command, the package/version observed before removal when safely discoverable, and safe before/after plugin-roster evidence. Do not record credentials or official file contents.",
    "After uninstall, the real local Harness must remain without dsh-plugin-image-mind/@ran-sh/dsh-vision for the rest of this task and after completion. Do not run any real-profile add/install/reinstall command in this task.",
    "Prove the real Harness itself still reaches its normal web readiness after dsh-vision is removed. This is a host-health check only; do not alter host files to make it pass.",
    "Repository typecheck, build, full tests, package integrity, and built-artifact verification all pass or are reported truthfully.",
    "The compatibility verdict must target exact @deepseek-ai/dsh@0.1.1-rc.1 and published dsh-plugin-image-mind@0.2.0; do not use moving dist-tags for the verdict.",
    "All compatibility-test Harness mutation after the initial real uninstall occurs only inside newly created task-owned temporary directories through official DSH commands.",
    "Before and after execution, record read-only hashes/metadata for a bounded set of relevant non-secret real Harness files if they exist, sufficient to prove that no direct/manual Harness-file mutation occurred; never record file contents or secrets. Expected official-plugin-manager lifecycle changes caused solely by the authorized uninstall must be distinguished from prohibited manual edits.",
    "Use pnpm pre-resolution in a temporary launcher to bypass the already-proven npm/npx transient resolver defect. Do not classify that known resolver behavior as an rc.1 plugin incompatibility.",
    "Official plugin add in the isolated Harness state must either complete successfully or produce bounded redacted evidence identifying the exact plugin/DSH compatibility failure.",
    "Exact rc.1 web readiness in the isolated state must be established by a real listener/HTTP readiness signal, not by assuming a long-running process is healthy.",
    "Prove that both the @ran-sh/dsh-vision service layer and dsh-plugin-image-mind provider/tool layer compose in the isolated rc.1 environment and that understand_image is registered/available.",
    "If a usable provider/keyless endpoint is safely available without writing/copying real Harness settings, execute one real image flow and record safe route/provider/model/trace evidence without image bytes, paths, signed URLs, prompts, keys, or provider response secrets. If no such provider is safely available, mark only this sub-check PARTIAL/BLOCKED and continue all keyless compatibility checks.",
    "Official plugin remove in the isolated state must leave no dsh-vision/image-mind bundle layer or task-owned plugin installation behind; report exact residuals if cleanup fails.",
    "No dsh-vision source, tests, scripts, CI, package metadata, compatibility docs, or other product files may be changed by this TEST_ONLY task.",
    "The Result Contract must state a plain-language rc.1 compatibility verdict: PASS, PARTIAL, or FAIL; separate Harness/plugin compatibility from provider-credential availability and from the known npm/npx resolver issue.",
    "The Result Contract must explicitly state `real_local_installation_after_task: absent` (or equivalent evidence-backed wording). Reinstalling dsh-vision into the user's real Harness is deferred to a future explicit final-install task after the best version is selected.",
    "The Result Contract must use schema_version 2, second-precision timezone-aware timeline.started_at/timeline.completed_at, and validator-owned result_validation evidence from `node .agent-workflow/validator/validate-contract.mjs result docs/agent-results/DSH-VISION-RC1-ISOLATED-COMPAT-006-result.json --stamp` after completion time is final.",
    "On completion, delete docs/agent-tasks/ACTIVE_TASK.json and commit/push only the Result Contract plus ACTIVE task deletion."
  ],
  "result_contract": "docs/agent-results/DSH-VISION-RC1-ISOLATED-COMPAT-006-result.json",
  "completion_commit_contract": [
    "docs/agent-results/DSH-VISION-RC1-ISOLATED-COMPAT-006-result.json",
    "docs/agent-tasks/ACTIVE_TASK.json"
  ],
  "delete_active_task_on_completion": true,
  "metadata": {
    "executor": "CODEX",
    "purpose": "uninstall-real-then-isolated-dsh-0.1.1-rc.1-compatibility",
    "related_issue": "21",
    "compatibility_target": "@deepseek-ai/dsh@0.1.1-rc.1",
    "plugin_target": "dsh-plugin-image-mind@0.2.0",
    "real_install_policy": "remove-now-reinstall-only-final-approved-version",
    "prepared_from_commit": "6f1e447b867359606ffb75e7ae2cd3484f65e2a3"
  }
}
