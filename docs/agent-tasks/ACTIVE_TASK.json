{
  "id": "DSH-VISION-RC1-BROWSER-REGRESSION-008",
  "mode": "TEST_ONLY",
  "source_branch": "main",
  "source_commit": "LATEST",
  "objective": "Validate the current unpublished dsh-vision/image-mind 0.2.x source against exact @deepseek-ai/dsh@0.1.1-rc.1 in a fully isolated temporary environment, focusing on real rc.1 browser/session behavior that GitHub CI cannot prove. The real local Harness must remain read-only and dsh-vision/image-mind must remain uninstalled after the task.",
  "context": "This is a fresh successor to deferred task 007, regenerated from the latest main after PRs #38, #39, #44, #45 and #46. The cohesive scope is real browser/session regression: privacy-preserving sessionBatchOffset history routing, restart recovery, the >8-image client fail-fast, and rollback of a failed/uncommitted routing batch without deleting Harness-owned attachment bytes. The current 0.2.0 packages are still unpublished, so this task proves source-built behavior only and MUST NOT claim registry compatibility or publish anything. All mutation must happen in task-owned temporary launcher/cache/DSH_HOME/browser/stub state outside the real Harness. Use exact @deepseek-ai/dsh@0.1.1-rc.1, preferably through a pre-resolved pnpm launcher to avoid the already-proven transient npm/npx Arborist stall. If a browser/main-model/provider subcheck cannot run without unsafe access to real settings or credentials, mark only that subcheck PARTIAL/BLOCKED and continue every safe keyless/local-stub check.",
  "allowed_changes": [
    "docs/agent-results/DSH-VISION-RC1-BROWSER-REGRESSION-008-result.json"
  ],
  "forbidden_changes": [
    "packages/**",
    "scripts/**",
    "tests/**",
    ".github/**",
    "README.md",
    "CHANGELOG.md",
    "docs/compatibility.md",
    "package.json",
    "package-lock.json",
    "manual or direct edits to any real DSH_HOME file or directory",
    "manual or direct edits to any real DeepSeek Harness profile, manifest, lockfile, settings file, credential file, cache, node_modules tree, plugin tree, or other Harness-owned state",
    "any real Harness plugin add/install/reinstall/remove action for dsh-vision/image-mind",
    "package publishing, npm dist-tag changes, tags, releases, or version bumps",
    "writes to Ran-sh/dsh-crew or any repository other than Ran-sh/dsh-vision",
    "credentials, API keys, secret values, signed URL secrets, attachment ids, or unredacted secret-bearing logs",
    "unrelated files"
  ],
  "validation": [
    "node .agent-workflow/validator/validate-contract.mjs task docs/agent-tasks/ACTIVE_TASK.json",
    "record exact source commit, OS, Node, npm, pnpm, and exact @deepseek-ai/dsh@0.1.1-rc.1 versions",
    "read-only verify at task start that the real local Harness does not actively compose dsh-plugin-image-mind or @ran-sh/dsh-vision; do not modify real Harness state even if inert residues exist",
    "npm run typecheck",
    "npm run build",
    "npm test",
    "npm run test:package",
    "npm run test:built",
    "create task-owned temporary launcher/cache/DSH_HOME/source-package/browser state outside the repository; load the current source-built plugin into exact rc.1 using an isolated mechanism and record that mechanism truthfully",
    "prove the isolated composition uses artifacts built from the exact recorded source commit rather than the published 0.1.x plugin line",
    "exercise session history with distinct generated image batches A then B; verify implicit latest selection resolves B while sessionBatchOffset 1 resolves A using deterministic fixture bytes/fingerprints or a local-stub observation without exposing attachment ids",
    "restart the isolated host/runtime while preserving only task-owned temporary DSH_HOME/attachment/index state and verify the committed earlier batch remains resolvable by sessionBatchOffset after restart",
    "in the actual isolated rc.1 browser/client, attempt to send MAX_IMAGES_PER_REQUEST + 1 draft images and verify fail-fast occurs before /image-mind upload, session message admission, preview commit, routing commit, or draft release; verify an actionable max-8 error and retained drafts",
    "verify a valid batch of exactly 8 images is not rejected by the image-count guard; it may stop at a later main-model/provider boundary if no safe credential is available",
    "exercise one failed-send routing rollback in isolated state: begin from a committed batch A, stage/upload a distinct batch B, force a post-upload send/prompt failure using only task-owned browser/network/stub controls, then verify B is discarded from image-mind routing and implicit latest selection resolves A again",
    "for the failed-send rollback, verify task-owned Harness attachment bytes for B are not deleted by dsh-vision/image-mind; report only safe counts/fingerprints, never raw ids or paths",
    "verify normal rendered conversation/UI/call views do not expose raw attachment ids, sha256 ids, local paths, signed URL query material, route telemetry, persisted batch ids, or credential data during these checks",
    "if a safe keyless/local stub OpenAI-compatible vision endpoint can be used, route at least one current-source understand_image call through it and record only redacted request-count/route/model/provider evidence; do not use paid/unknown real credentials blindly",
    "delete all task-owned temporary launcher/cache/DSH_HOME/package/browser/stub-server artifacts after evidence capture",
    "final read-only verification that the real local Harness still does not actively compose dsh-plugin-image-mind/@ran-sh/dsh-vision and was not modified by this task"
  ],
  "acceptance_criteria": [
    "The Task Contract validates before execution and the exact source SHA is recorded in the Result Contract.",
    "The real local Harness remains read-only for the entire task and dsh-vision/image-mind remains uninstalled there after completion.",
    "All source-built/runtime/browser testing that requires mutation occurs only in newly created task-owned temporary state outside the repository and outside the real Harness state.",
    "The Result clearly distinguishes source-built 0.2.x regression evidence from published-package/registry compatibility; do not claim 0.2.0 registry compatibility from this task.",
    "Repository typecheck, build, tests, package integrity, and built-artifact checks pass or failures are reported truthfully without product-code fixes in this TEST_ONLY task.",
    "A/B history routing proves latest batch selection and previous-batch selection with distinct visual fixtures, with sessionBatchOffset 1 selecting A after B is newer. Do not infer success from metadata alone when deterministic byte/fingerprint or local-stub evidence is available.",
    "Restart recovery proves previously committed session image history can be resolved after process/runtime restart using only task-owned persisted attachment/index state.",
    "For the 9-image browser case, PASS requires evidence that rejection happens before upload/message/routing/preview side effects and drafts are retained. If actual rc.1 browser automation is unavailable, mark this subcheck PARTIAL rather than substituting existing unit tests as real-browser evidence.",
    "For the exactly-8 browser case, prove the count guard allows the batch to proceed beyond the guard. A later credential/provider/model blocker is allowed and must be reported separately.",
    "Failed-send rollback PASS requires evidence that an uncommitted failed batch stops being the implicit latest routing batch and the previous committed batch becomes latest again, while dsh-vision/image-mind does not delete Harness-owned attachment bytes.",
    "No user-visible or captured diagnostic evidence may leak raw attachment ids, sha256 ids, local paths, signed URL query values, credentials, route telemetry, or persisted batch ids.",
    "If a local keyless stub provider is used, successful current-source understand_image evidence must be attributable to the isolated current 0.2.x artifacts and exact rc.1 composition; record safe route/provider/model/request-count evidence only.",
    "No package/release/version/publish action is allowed.",
    "The Result Contract must state a plain-language verdict for each of: source-built rc.1 composition, A/B session history routing, restart recovery, 9-image browser fail-fast, exactly-8 guard allowance, failed-send routing rollback, privacy/redaction, and optional local-stub vision flow.",
    "The Result Contract must explicitly state that the real local installation remains absent and real Harness state was not modified.",
    "The Result Contract must use schema_version 2, second-precision timezone-aware timeline.started_at/timeline.completed_at, and validator-owned result_validation evidence from `node .agent-workflow/validator/validate-contract.mjs result docs/agent-results/DSH-VISION-RC1-BROWSER-REGRESSION-008-result.json --stamp` after completion time is final.",
    "On completion, delete docs/agent-tasks/ACTIVE_TASK.json and commit/push only the Result Contract plus ACTIVE task deletion."
  ],
  "result_contract": "docs/agent-results/DSH-VISION-RC1-BROWSER-REGRESSION-008-result.json",
  "completion_commit_contract": [
    "docs/agent-results/DSH-VISION-RC1-BROWSER-REGRESSION-008-result.json",
    "docs/agent-tasks/ACTIVE_TASK.json"
  ],
  "delete_active_task_on_completion": true,
  "metadata": {
    "executor": "CODEX",
    "purpose": "isolated-rc1-browser-session-regression",
    "related_issue": "21",
    "compatibility_target": "@deepseek-ai/dsh@0.1.1-rc.1",
    "source_line": "current-0.2.x-unpublished",
    "real_install_policy": "remain-uninstalled-until-final-approved-version",
    "supersedes_deferred_task": "DSH-VISION-RC1-BROWSER-REGRESSION-007",
    "prepared_from_commit": "6e4fb0cdda795265500d072501167384fa95e686"
  }
}
