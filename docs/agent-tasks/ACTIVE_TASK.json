{
  "id": "DSH-VISION-EVIDENCE-QUALITY-BENCHMARK-015",
  "mode": "IMPLEMENT",
  "source_branch": "main",
  "source_commit": "LATEST",
  "objective": "Continue and finish Batch 015 on existing Draft PR #57 by correcting benchmark answer leakage, closing automatic narrow-refocus behavior, executing the required historical 3072-long-edge comparison, then rerunning the corrected frozen benchmark and full CI without weakening thresholds.",
  "context": "The first Batch 015 executor pass created branch feat/evidence-quality-benchmark-batch-015 and pushed a 100-case corpus, deterministic understand_image replay, evidence-cache benchmark results, long-image fixtures/tests, and a validator-stamped PARTIAL Result Contract. ChatGPT opened Draft PR #57 because the executor's local GitHub session was unauthenticated. Review found three substantive acceptance gaps that must be fixed in this same batch/PR: (1) benchmark prompts leak expected answer tokens such as UI-FACT-01 while assertions require the same token, so the visual-quality gate is contaminated; (2) the narrow-refocus test manually supplies cache=no-store only after first proving the narrow question returns evidence-cache, so automatic insufficient-evidence refocus is not proven; (3) the required historical 3072-long-edge preprocessing comparison was explicitly SKIP and current tests only comment on old geometry rather than executing a faithful baseline comparison. Preserve all existing Batch 015 work that remains valid, continue PR #57 non-destructively, and do not split a micro-follow-up. Deterministic marker-backed local vision may be used for cache/telemetry correctness but must not be presented as real provider visual-quality evidence. No authorized real/local visual provider credential is required; if none is explicitly available, that subcheck may remain SKIP/PARTIAL and must never be obtained from real Harness settings.",
  "allowed_changes": [
    "packages/vision/src/**",
    "packages/vision/tests/**",
    "packages/image-mind/src/**",
    "packages/image-mind/tests/**",
    "tests/**",
    "benchmarks/**",
    "scripts/**",
    "package.json",
    "package-lock.json",
    "packages/vision/package.json",
    "packages/image-mind/package.json",
    "tsconfig*.json",
    "packages/**/tsconfig*.json",
    "vitest*.ts",
    "packages/**/vitest*.ts",
    ".github/workflows/ci.yml",
    ".github/workflows/dsh-compat.yml",
    "docs/agent-results/DSH-VISION-EVIDENCE-QUALITY-BENCHMARK-015-result.json"
  ],
  "forbidden_changes": [
    "README.md, CHANGELOG.md, docs/compatibility.md, docs/verification-debt.md, roadmap/history documentation, or release notes except the required Result Contract and ACTIVE_TASK deletion",
    "version bumps, npm publishing, npm dist-tags, repository tags/releases, or final-install actions",
    "manual/direct edits to any real or isolated Harness-owned state, settings, credential files, caches, node_modules/plugin trees, or pnpm store entries",
    "credentials, API keys, bearer tokens, cookies, signed URL secrets, attachment ids, private local paths, or secret-bearing logs",
    "reading real Harness settings/credential stores to obtain provider credentials",
    "writes to repositories other than Ran-sh/dsh-vision",
    "loosening benchmark regression thresholds solely to make the candidate pass",
    "unrelated provider/release work reserved for other batches",
    "force-push, history rewrite, destructive reset/clean of unrelated user work"
  ],
  "validation": [
    "node .agent-workflow/validator/validate-contract.mjs task docs/agent-tasks/ACTIVE_TASK.json",
    "fetch latest main and existing branch feat/evidence-quality-benchmark-batch-015 / Draft PR #57; preserve all existing Batch 015 commits and non-destructively merge or fast-forward the exact validated latest main task commit into the execution branch before changes; do not force-push or rewrite history",
    "confirm the execution branch contains this revised ACTIVE_TASK contract before implementation and keep PR #57 Draft/unmerged until ChatGPT review",
    "benchmark anti-leak: regenerate or edit all 100 frozen cases so no prompt contains any expected containsAll/excludes token, expected OCR answer, or semantically equivalent answer-bearing fixture identifier; case ids may stay stable but must be opaque to the model with respect to expected visual facts",
    "add a deterministic regression test that scans every case and fails if any expected positive/negative assertion token is present in the corresponding prompt; also guard against generator templates reintroducing the leak",
    "regenerate corpus manifest/results/reports from the corrected corpus; baseline and candidate must both run through the real understand_image tool path with the same deterministic provider/settings; re-run score and compare using the existing DEFAULT_BENCHMARK_THRESHOLDS without loosening them",
    "keep deterministic marker decoding scoped to routing/cache/telemetry correctness; do not call it real visual-model quality. If no explicitly authorized visual provider is available, provider visual-quality remains SKIP/PARTIAL while deterministic expected-fact quality is used only to validate cache equivalence and telemetry",
    "automatic narrow-refocus: reproduce a broad reusable-evidence call followed by a materially narrow question whose required visual fact is absent from the cached broad evidence. The final system behavior must not silently terminate on the insufficient cached evidence or guess. It must cause a focused fresh visual read with truthful cache=no-store/provider traffic when needed, while preserving the prior broad reusable evidence for later broad reuse",
    "if current product logic cannot determine/refocus insufficient evidence, implement the smallest safe Codex-owned mechanism in packages/image-mind within this same batch and add regression coverage for the automatic decision path; a test that merely makes a third manual cache=no-store call is insufficient",
    "verify semantic-cache/evidence-cache/provider route.source, route.task, cacheMode, providerCalls, cacheHits, selected provider/model, provider-health neutrality, and trace remain truthful across the automatic refocus flow",
    "historical preprocessing comparison: on the same deterministic 1440x10000 and approximately 1440x20000 text-heavy fixtures, execute current aspect/pixel-aware preprocessing and a faithful benchmark-only historical 3072-long-edge baseline; record dimensions, output bytes, deterministic latency/processing measure, and readability/order assertions for both",
    "the historical comparison must be executable evidence, not a comment or arithmetic estimate. Keep current policy only if it is at least as good on deterministic readability/order under acceptable memory/latency bounds; do not change production preprocessing merely to mimic the old baseline",
    "re-run evidence reuse, no-store, refresh, explicit provider/model, settings invalidation, ordinary photo/general, multi-image ordering/split regressions, routeSourceOutcomes raw-record reconciliation, long-image preprocessing, and controlled benchmark failure",
    "run npm run typecheck, npm run build, npm test, npm run test:package, npm run test:built, npm run benchmark:corpus, npm run benchmark:deterministic, benchmark:score on corrected baseline/candidate outputs, benchmark:compare pass case, and a controlled failing comparison that exits non-zero",
    "require final PR #57 head GitHub CI to pass all applicable Node22, Node24, Windows, package/built, and secret-scan jobs before completion",
    "update the existing docs/agent-results/DSH-VISION-EVIDENCE-QUALITY-BENCHMARK-015-result.json with truthful revised evidence/status, including PR #57 and final CI; preserve external provider-quality SKIP/PARTIAL if credentials are unavailable",
    "delete docs/agent-tasks/ACTIVE_TASK.json on the execution branch only after all required checks complete, then run node .agent-workflow/validator/validate-contract.mjs result docs/agent-results/DSH-VISION-EVIDENCE-QUALITY-BENCHMARK-015-result.json --stamp, commit/push, leave PR #57 unmerged, and stop"
  ],
  "acceptance_criteria": [
    "Task Contract validates; existing PR #57 is the sole Batch 015 execution vehicle and prior valid work is preserved non-destructively.",
    "The 100-case corpus retains the required category totals and contains zero expected-answer leakage from assertions into prompts; an automated anti-leak test enforces this.",
    "Corrected baseline/candidate outputs are regenerated from the leak-free corpus and pass existing benchmark thresholds; controlled wrong answers still fail comparison.",
    "Layered evidence reuse occurs only when cached broad evidence is sufficient. A narrow question requiring an absent detail causes automatic focused fresh no-store visual traffic instead of silently returning insufficient cached evidence or guessing, and broad evidence remains reusable afterward.",
    "Provider/semantic/evidence route and trace telemetry stay truthful and cache hits do not create synthetic provider-health success across the refocus flow.",
    "The historical 3072-long-edge baseline is actually executed against the same long-page fixtures and compared with current preprocessing using dimensions/bytes/processing/readability/order evidence; no regression is justified by assertion alone.",
    "Deterministic marker-backed evidence is described only as deterministic cache/telemetry/equivalence evidence; lack of an authorized real visual provider remains an honest external SKIP/PARTIAL, not a reason to read credentials or fabricate visual-quality claims.",
    "All focused/full repository/package/built benchmark gates and final PR #57 CI pass on the final head.",
    "Result Contract v2 is updated, second-precision timezone-aware, validator-stamped, and truthfully records any remaining external provider limitation.",
    "On completion ACTIVE_TASK is deleted on the execution branch, PR #57 remains unmerged, and Codex stops without self-assigning Batch 016."
  ],
  "result_contract": "docs/agent-results/DSH-VISION-EVIDENCE-QUALITY-BENCHMARK-015-result.json",
  "completion_commit_contract": [
    "docs/agent-results/DSH-VISION-EVIDENCE-QUALITY-BENCHMARK-015-result.json",
    "docs/agent-tasks/ACTIVE_TASK.json"
  ],
  "delete_active_task_on_completion": true,
  "metadata": {
    "executor": "CODEX",
    "purpose": "layered-evidence-visual-quality-benchmark",
    "related_issue": "21",
    "roadmap_issue": "54",
    "resume_pr": "57",
    "resume_branch": "feat/evidence-quality-benchmark-batch-015",
    "handoff_policy": "codex-owns-all-implementation",
    "benchmark_cases_target": 100,
    "continuation_reason": "review-found-answer-leak-refocus-and-historical-baseline-gaps",
    "next_planned_batch": "016"
  }
}