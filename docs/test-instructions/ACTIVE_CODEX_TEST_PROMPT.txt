dsh-vision / image-mind — ACTIVE CODEX DSH TEST PROMPT
Version: 2026-08-20
Repository: Ran-sh/dsh-vision
Mode: TEST ONLY
Real backend: Opencode Go package → Muse API

======================================================================
0. READ THIS FIRST
======================================================================

You are the independent verification engineer for Ran-sh/dsh-vision.

Your job is to TEST the current repository in a real DeepSeek Harness (DSH)
environment and produce a structured Markdown report for later analysis.

You are NOT the developer in this run.

Do not fix bugs.
Do not refactor.
Do not edit implementation code.
Do not edit existing tests to make them pass.
Do not weaken assertions.
Do not change package versions.
Do not create a PR.
Do not publish packages.

The only repository changes allowed at the END of this run are:

1. create/update:
   docs/test-results/dsh-vision-codex-test-report.md

2. delete this active instruction file:
   docs/test-instructions/ACTIVE_CODEX_TEST_PROMPT.txt

No other tracked file may be committed.

Before committing the report, run:

    git diff --cached --name-only

The staged file list must contain ONLY:

    docs/test-results/dsh-vision-codex-test-report.md
    docs/test-instructions/ACTIVE_CODEX_TEST_PROMPT.txt

where the second path is a deletion.

If any source/test/config file appears in staging, STOP and unstage it.
Do not repair it.

After the final report commit, push it so ChatGPT can read the report directly
from GitHub. The active prompt must be gone after the test run.

======================================================================
1. REPOSITORY CONTEXT TO READ
======================================================================

Before testing, read:

    README.md
    CHANGELOG.md
    docs/verification-debt.md
    docs/codex-dsh-real-test-plan.md
    benchmarks/vision/README.md
    package.json

Treat docs/verification-debt.md as the durable verification checklist.
Do not modify it. Map each item to PASS / FAIL / PARTIAL / SKIP in the final
report.

Main packages:

    packages/vision
        @ran-sh/dsh-vision
        owns ctx.vision

    packages/image-mind
        dsh-plugin-image-mind
        Provider + Tool + UI plugin

======================================================================
2. REAL API ENVIRONMENT — IMPORTANT
======================================================================

The only currently usable real API path is:

    Opencode Go package → Muse API

Do NOT describe Muse as a second independent provider.
Do NOT claim multiple independent gateways.

Real independent gateway count for this run:

    1

The Muse API exposes multiple models. Discover actual available model IDs.
Prioritize at least:

    Model A: an actually available MiMo vision-capable model
    Model B: an actually available Qwen / 千问 vision-capable model

If another stable vision-capable model is available, it may be tested as
Model C.

Never invent model IDs. Use model discovery, current DSH configuration, or the
actual Muse API model list. Record the complete model ID in the report.

If a model is listed but fails visual input, record the failure. Do not mark it
PASS merely because discovery listed it.

======================================================================
3. SINGLE-GATEWAY CONCLUSION BOUNDARY
======================================================================

You may configure multiple provider entries that share the same Muse API, e.g.:

    muse-mimo
    muse-qwen

with the same baseURL/credential but different models.

This is useful for testing routing, fallback plumbing, selector behavior,
health state, circuit state, explicit provider/model pinning, and trace
counters.

However all such tests MUST be labeled:

    SINGLE-GATEWAY SIMULATION

Never report:

    REAL CROSS-PROVIDER PASS

for this environment.

A correct conclusion is:

    provider fallback routing logic validated under single-gateway Muse API
    simulation; independent provider failover remains unverified.

======================================================================
4. SOURCE PROTECTION
======================================================================

At the beginning run:

    git rev-parse HEAD
    git branch --show-current
    git status --short

Record:

    source commit SHA
    branch
    dirty-before state

At the end, before staging the report, run:

    git status --short

Codex must not intentionally change:

    packages/vision/**
    packages/image-mind/**
    scripts/**
    tests/**
    package.json
    package-lock.json
    tsconfig*
    README.md
    CHANGELOG.md
    docs/verification-debt.md
    GitHub Actions

Build-generated files may appear; note them in the report. Do not commit them.
Do not delete pre-existing user changes.

======================================================================
5. SECRET SAFETY
======================================================================

Never write real secrets into terminal summaries, reports, commits, fixtures,
or benchmark outputs.

Do not expose:

- API keys
- Authorization headers
- Bearer tokens
- cookies
- credential-file contents
- signed URL query strings
- secret-bearing error bodies

Allowed:

    Muse API credential: PRESENT

Not allowed:

    Muse API credential: <actual key>

Redact sensitive values as:

    [REDACTED]

Record only the base URL host when possible.

======================================================================
6. PHASE A — STATIC / BUILD
======================================================================

Run in order:

A1  npm ci
A2  npm run typecheck
A3  npm test
A4  npm run build
A5  npm run test:package
A6  npm run test:built

Do not stop the entire run just because one command fails if later commands are
still meaningful.

For each record:

    result
    duration
    exit code
    key evidence

======================================================================
7. PHASE B — REAL DSH INTEGRATION
======================================================================

Use an isolated test profile where reasonably possible. Avoid polluting the
user's main DSH profile.

Validate:

B1  plugin install
B2  DSH startup
B3  @ran-sh/dsh-vision service load
B4  image-mind plugin load
B5  settings UI opens
B6  provider configuration loads
B7  model discovery
B8  provider/model save workflow
B9  plugin remove
B10 reinstall
B11 no stale bundle layer
B12 DSH restart
B13 plugin remains usable after restart

Record DSH version, profile, source SHA, package version, evidence, and any
redacted failures.

======================================================================
8. PHASE C — MUSE MODEL DISCOVERY
======================================================================

Identify:

- Muse API base URL host
- apiStyle
- credential status
- discovery success/failure
- actual vision-capable model IDs

At minimum test one MiMo model and one Qwen/千问 model if available.

Recommended provider entries for controlled comparison:

    muse-mimo
    muse-qwen

Reuse equivalent existing entries if already configured.

======================================================================
9. PHASE D — REAL VISUAL QUALITY
======================================================================

Use identical images and prompts across models.

D1 PHOTO

Prompt:

    Describe only the important visible facts in this image.
    Do not guess details that are not visible.

D2 CHINESE OCR

Prompt:

    逐字识别图片中所有可见文字。
    保留数字、标点、大小写与有意义的换行。
    看不清的部分写 [无法辨认]，不要猜。

D3 ENGLISH OCR

Prompt:

    Transcribe all visible text exactly.
    Preserve punctuation, casing, numbers and useful line breaks.
    Mark illegible spans instead of guessing.

D4 CODE SCREENSHOT

Prompt:

    Read the visible code, terminal output, filenames,
    line numbers and error text exactly.
    Do not silently correct the code.

D5 UI SCREENSHOT

Prompt:

    Inspect hierarchy, layout, alignment, spacing,
    clipping, overflow, controls, labels and visible states.
    Tie every issue to concrete visible evidence.

D6 CHART

Prompt:

    Read the title, axes, units, legend, series,
    visible values, extrema and trend.
    Separate directly observed values from estimates.

D7 DOCUMENT

Prompt:

    Preserve headings, labels, fields, values, tables,
    reading order and footnotes.
    Do not invent missing fields.

For each case record:

- result
- provider entry
- actual model ID
- latency
- providerCalls
- payloadBytes
- cacheHits
- inputTokens
- outputTokens
- retries
- modelFallbacks
- providerFallbacks
- splits
- quality notes
- failure evidence

======================================================================
10. PHASE E — MIMO VS QWEN
======================================================================

Compare the same case set, same images, same prompts, same cache policy.

Measure at least:

- Chinese OCR accuracy
- English OCR accuracy
- code transcription
- UI small-detail recall
- document field recall
- chart numeric accuracy
- photo hallucination
- multi-image identity
- p50 latency
- p95 latency
- input tokens
- output tokens
- provider calls
- failure rate

Do not write only “MiMo is better” or “Qwen is better.” Provide case evidence.

======================================================================
11. PHASE F — MULTI-IMAGE
======================================================================

F1 2-image compare
F2 4 images
F3 8 images
F4 >8 images
F5 real 413 if reproducible

For compare use:

    Compare Image 1 and Image 2.
    Separate unchanged facts, additions, removals and modifications.
    Always identify which Image N supports each statement.

Validate stable original Image N identity and ordering.

For >8 images expect rejection before provider traffic; providerCalls should be
0 if trace is available.

If Muse can reliably trigger HTTP 413, validate recursive split, merged
evidence, stable Image N, trace.splits, providerCalls, and payloadBytes.

If not reproducible, report BLOCKED / NOT REPRODUCIBLE. Do not fake it.

======================================================================
12. PHASE G — SEMANTIC CACHE
======================================================================

G1 same image + same prompt twice
G2 cache=refresh
G3 cache=no-store

Check providerCalls, cacheHits, latency, and answer consistency.

Expected:

- normal second exact call may become providerCalls=0 on cache hit
- refresh must re-read pixels/backend
- no-store must bypass stored reuse

======================================================================
13. PHASE H — LAYERED EVIDENCE CACHE
======================================================================

Use OCR/document/UI/code/chart/screenshot/compare tasks.

Ask different questions about the same image, for example:

Q1  OCR all visible text.
Q2  What is the title?
Q3  What value appears in the bottom-right cell?

ProviderCalls=0 on later questions may be valid reuse, but quality is the
critical criterion.

Explicitly test narrow details:

- tiny icon
- one table cell
- one code line
- very small number
- tooltip
- corner status indicator

Correct behavior when cached evidence is insufficient:

    perform fresh/refocused visual analysis

Incorrect behavior:

    answer from incomplete cache by guessing

Record zero-provider reuse, stale answers, narrow-detail failures, and
hallucinations.

======================================================================
14. PHASE I — TASK-AWARE TOKEN BUDGET
======================================================================

Using the same Muse model, test OCR, document, code, UI, chart, photo, general.

Validate effective cap semantics:

    effectiveMaxOutputTokens = min(taskBudget, providerConfiguredCap)

Test both:

I1 provider cap above task budget
I2 provider cap below task budget

If wire values cannot be observed from real Muse traffic, use a LOCAL MOCK and
label it clearly.

======================================================================
15. PHASE J — OPENAI DETAIL COMPATIBILITY
======================================================================

Opencode Go Muse API is NOT automatically the official OpenAI API.

Only baseURL=https://api.openai.com/v1 counts as official OpenAI.

For Muse, mainly verify that OpenAI-only image detail fields do not break
compatible requests.

If no official OpenAI credential is available, mark official OpenAI detail
validation SKIP.

======================================================================
16. PHASE K — HTTP ERROR / RETRY
======================================================================

Do not burn Muse quota intentionally to create failures. Prefer LOCAL MOCK for
controlled deterministic cases.

K1 400: no retry, no provider fallback
K2 401: no retry, no automatic fallback
K3 403: same
K4 404: same
K5 429 + Retry-After: retry and trace.retries increments
K6 500: recoverable path
K7 503: recoverable path
K8 timeout
K9 network error
K10 invalid response shape

Label every result as one of:

    REAL MUSE
    LOCAL MOCK
    SINGLE-GATEWAY SIMULATION

======================================================================
17. PHASE L — MODEL OVERRIDE / MODEL FALLBACK
======================================================================

This is especially important because Muse exposes multiple models.

L1 explicit model override
L2 explicit model is sticky; no silent automatic substitution
L3 automatic model fallback under controlled compatible failure

Record final model and modelFallbacks.

Keep model fallback distinct from provider fallback.

======================================================================
18. PHASE M — PROVIDER FALLBACK (SIMULATION ONLY)
======================================================================

Example:

    provider A = Muse + MiMo
    provider B = Muse + Qwen

Same gateway, different models.

M1 A failure → B for eligible recoverable failure
M2 explicit provider A → no B
M3 explicit model → no provider substitution
M4 deterministic 400/404 → no fallback
M5 auth failure → no fallback
M6 timeout/429/5xx → eligible
M7 fallback-count bound

Everything here must say:

    SINGLE-GATEWAY SIMULATION

======================================================================
19. PHASE N — HEALTH / CIRCUIT BREAKER
======================================================================

Use LOCAL MOCK or SINGLE-GATEWAY SIMULATION.

N1 initial order
N2 recoverable failure streak
N3 threshold → circuit open
N4 cooldown exclusion
N5 half-open
N6 success → closed
N7 half-open failure → reopen
N8 400/401/model incompatibility must not poison health

Record selection order, streak, circuit state, cooldown behavior.

Independent provider isolation remains NOT FULLY VALIDATED.

======================================================================
20. PHASE O — CANCELLATION
======================================================================

O1 caller abort during in-flight request
O2 caller abort during retry sleep

Expected:

- stop promptly
- no extra retry
- no extra fallback
- retry counter does not falsely increment
- caller cancellation does not degrade provider health

======================================================================
21. PHASE P — RUNTIME LIFECYCLE OBSERVER
======================================================================

Subscribe to:

    VisionRuntime.subscribeLifecycle()

Successful call:

    started → completed

Failed call:

    started → failed

Check metadata:

started:
- requestId
- provider
- task
- imageCount
- cacheMode
- maxOutputTokens
- explicitProvider
- explicitModel
- startedAt

completed:
- elapsedMs
- resultProvider
- model
- usage
- trace

failed:
- elapsedMs
- errorCode
- aborted
- trace

Security audit: lifecycle events must NOT contain:

- raw prompt
- image bytes/base64
- local paths
- attachment IDs
- endpoint URLs
- API keys
- Authorization
- provider response text
- raw error message/cause

Also register an observer that throws. Verify observer failure does not fail the
vision call.

======================================================================
22. PHASE Q — BROWSER / DSH UI
======================================================================

Use real DeepSeek Harness UI where available.

Q1 drag PNG
Q2 paste PNG
Q3 JPEG
Q4 WebP
Q5 multi-image
Q6 image-only message
Q7 image + text
Q8 failed-send draft retention
Q9 retry
Q10 hidden routing hint not visible
Q11 hidden attachment metadata not visible
Q12 preview
Q13 restart DSH
Q14 ask about old image after restart
Q15 old attachment can be reread
Q16 refresh
Q17 no-store

Keep screenshots/log evidence when useful, with secrets redacted.

======================================================================
23. PHASE R — LONG SCREENSHOT
======================================================================

At least test:

R1 1440x10000 PNG
R2 around 1440x20000 or another reasonably generated very long page

Prefer content containing Chinese, English, small text, tables, numbers,
multiple columns, and important content near both top and bottom.

Check:

- horizontal text readability
- OCR recall
- omissions
- over-downscaling
- upload latency
- browser memory anomalies
- providerCalls
- token usage

Use cache=refresh to avoid stale cache interference.

======================================================================
24. PHASE S — BENCHMARK
======================================================================

Available commands:

    npm run benchmark:run
    npm run benchmark:score
    npm run benchmark:compare

Do not fabricate a low-quality 100-case corpus merely to hit a number.
Start with a useful smoke corpus, approximately:

    OCR       3
    UI        3
    Code      3
    Document  3
    Chart     3
    Photo     3
    Compare   2

Run the SAME corpus once with Muse+MiMo and once with Muse+Qwen.

benchmark:run is TOOL-DIRECT. It tests:

    understand_image → ctx.vision → adapter → Muse API

It does NOT prove that the main DeepSeek agent chose the tool correctly.
Agent routing must be tested separately.

Record:

- routingSuccessRate
- assertionPassRate
- taskSuccessRate
- forbiddenHitCount
- traceCoverage
- tokenUsageCoverage
- zeroProviderReuseRate
- p50/p95 latency
- providerCalls
- payloadBytes
- inputTokens
- outputTokens
- cacheHits
- retries
- modelFallbacks
- providerFallbacks
- splits

Run benchmark:compare if a comparable baseline exists.

======================================================================
25. PHASE T — AGENT ROUTING
======================================================================

Use real DSH conversation flow.

T1 image only: “看看这张图。”
T2 OCR: “这张图里写了什么？”
T3 UI: “这个界面哪里有问题？”
T4 vague: “帮我看看这个。”
T5 multi-image: “对比这两张图有什么变化。”

Record:

- whether understand_image was actually called
- tool call count
- duplicate calls
- whether final answer is supported by visual evidence
- whether the agent answered without looking at the image

Keep this section separate from TOOL-DIRECT benchmark results.

======================================================================
26. FINAL REPORT PATH AND FORMAT
======================================================================

Create/update exactly:

    docs/test-results/dsh-vision-codex-test-report.md

Required report sections:

# dsh-vision Codex Real DSH Test Report

## 1. Test Identity
Repository
Branch
Source Commit SHA
Report Commit SHA (fill after commit if practical; otherwise leave pending and
state it in final chat)
Dirty Before
Dirty After
Date
OS
Node
npm
DSH Version
Codex Environment
Test Mode: TEST ONLY / REPORT-ONLY COMMIT

## 2. Real API / Model Matrix
Gateway: Opencode Go package → Muse API
Real independent gateway count: 1
Credential: PRESENT / MISSING
Provider entry, host, apiStyle, full model ID, family, capability, discovery
source, status

## 3. Static / Build Results
ID | Command | Result | Duration | Evidence

## 4. DSH Integration
ID | Scenario | Result | Evidence | Notes

## 5. MiMo Real Model Results
Case-level metrics and quality evidence

## 6. Qwen Real Model Results
Same format

## 7. MiMo vs Qwen
Metric | MiMo | Qwen | Better | Evidence

## 8. Cache
Semantic cache
Layered evidence
refresh
no-store
zero-provider reuse
stale answers
narrow-detail failures
hallucination under reuse

## 9. Fallback / Retry / Circuit
Every item labeled REAL MUSE / LOCAL MOCK / SINGLE-GATEWAY SIMULATION / SKIP

## 10. Image Pipeline
PNG
JPEG
WebP
Long screenshot
Multi-image
8 images
>8 rejection
413
Stable Image N

## 11. Lifecycle / Trace / Usage
Lifecycle
Secret-content audit
Trace reconciliation
Token reconciliation
Explicitly state whether prompt/path/key/response text leaked into lifecycle
metadata

## 12. Benchmark
Corpus
Mode: TOOL-DIRECT
MiMo score
Qwen score
All major scorer metrics
Regression gate PASS/FAIL/NOT RUN
Failed gates

## 13. Agent Routing
Case-by-case tool routing evidence

## 14. Failures
For each real failure:
ID
Severity
Area
Reproduction
Expected
Actual
Evidence
Likely layer
Possible cause (optional)

Do NOT implement a fix.

## 15. Skipped / Blocked
ID
Reason
Required later

Include lack of independent second provider, unavailable official OpenAI,
unreproducible 413, model quota, missing browser or credential when applicable.

## 16. Verification Debt Mapping
Map every docs/verification-debt.md item to PASS / FAIL / PARTIAL / SKIP with
evidence.

## 17. Final Verdict
Static/build
DSH integration
Muse MiMo
Muse Qwen
Cache
Model fallback
Provider fallback simulation
Circuit
Trace
Usage
Lifecycle
Long screenshot
Agent routing
Benchmark

Overall verdict must be one of:

    READY FOR NEXT DEVELOPMENT ITERATION

or

    NOT READY — FIX BLOCKERS FIRST

Include counts:

    Blocker
    High
    Medium
    Low
    Skipped

======================================================================
27. SEVERITY
======================================================================

BLOCKER examples:
- cannot install
- DSH cannot start
- plugin cannot load
- image main flow unusable
- secret leak
- main-flow crash

HIGH examples:
- major OCR failure
- stale cache produces wrong answers
- explicit provider/model ignored
- deterministic 4xx retries/reroutes incorrectly
- caller abort continues requests
- attachment lost after restart
- Image N corrupted
- fallback routes incorrectly

MEDIUM examples:
- trace mismatch
- token usage mismatch
- one model compatibility failure
- long screenshot quality regression
- latency regression

LOW examples:
- wording
- minor UI issue
- non-critical logging issue

======================================================================
28. FINAL COMMIT AND SELF-DELETION
======================================================================

After testing and report generation:

1. Confirm no source file was intentionally changed.

2. Delete this active prompt file:

       docs/test-instructions/ACTIVE_CODEX_TEST_PROMPT.txt

3. Stage ONLY:

       docs/test-results/dsh-vision-codex-test-report.md
       docs/test-instructions/ACTIVE_CODEX_TEST_PROMPT.txt

   The second item must be staged as a deletion.

4. Run:

       git diff --cached --name-only

   If any other path appears, unstage it and do not include it.

5. Commit with a report-only message, for example:

       git commit -m "test: record real DSH vision verification"

6. Push the report commit.

Do not commit benchmark output, screenshots, build artifacts, source changes,
or local DSH profile data unless explicitly requested in a future prompt.

======================================================================
29. FINAL CODEX CHAT REPLY
======================================================================

Keep the final chat reply short:

Source Commit SHA:
Report Commit SHA:
Total Tests:
PASS:
FAIL:
SKIP/BLOCKED:
BLOCKER:
HIGH:
Overall Verdict:
Report Path:
    docs/test-results/dsh-vision-codex-test-report.md

Do not paste the full report into chat.

======================================================================
30. FINAL PRINCIPLE
======================================================================

The goal is not to make the project look healthy.
The goal is to accurately measure the current main branch in:

    real DeepSeek Harness
    + Opencode Go package
    + Muse API
    + actual MiMo/Qwen vision models

FAIL is valuable.
SKIP is valid.
PARTIAL is valid.
UNKNOWN is valid.
Fake PASS is forbidden.

TEST ONLY.
DO NOT FIX CODE.
THE ONLY COMMITTED OUTPUT IS THE TEST REPORT PLUS DELETION OF THIS ACTIVE PROMPT.
