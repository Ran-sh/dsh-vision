DSH-VISION CODEX TARGETED RETEST PROMPT

Protocol: Agent Handoff Protocol v1
Agent: CODEX
Mode: TEST_ONLY
Repository: Ran-sh/dsh-vision
Branch: main
Source: LATEST_MAIN
Previous implementation baseline before ACTIVE task creation: e1c0a6f851f7af75a9504b8eb31951bb5fb8fd77
Previous report: docs/test-results/dsh-vision-codex-test-report.md
Output report: docs/test-results/dsh-vision-codex-blocker-high-retest.md

============================================================
0. ROLE AND ABSOLUTE RULES
============================================================

You are the independent validation engineer for this run. You are NOT the developer.

TEST ONLY.

Do not fix source code. Do not refactor. Do not change existing tests or assertions. Do not loosen expectations. Do not change package versions, configs, build scripts, lockfiles, CI, README, CHANGELOG, or docs/verification-debt.md.

The only repository writes allowed are:

1. docs/test-results/dsh-vision-codex-blocker-high-retest.md
2. deletion of docs/agent-tasks/ACTIVE_CODEX_TASK.md when complete
3. deletion of docs/test-instructions/ACTIVE_CODEX_TEST_PROMPT.txt when complete

Temporary local logs/screenshots/build outputs are allowed for evidence but must not be committed by default.

Never print, commit, or report API keys, bearer tokens, Authorization headers, cookies, credential-file contents, signed URLs, or secrets. Record only statements such as:

Muse API credential: PRESENT

Redact secret-bearing error text.

A failure is evidence. Do not repair it.

Use only these states:
PASS / FAIL / PARTIAL / SKIP / BLOCKED / NOT RUN

============================================================
1. START-OF-TASK SAFETY
============================================================

Before testing:

git pull --ff-only
git rev-parse HEAD
git branch --show-current
git status --short

Record all four results in the report.

If the worktree already contains user changes, preserve them. Do not reset, clean, stash, delete, overwrite, or include them in your completion commit.

Read in this order:

1. docs/agent-workflow.md
2. docs/agent-tasks/ACTIVE_CODEX_TASK.md
3. this file
4. docs/test-results/dsh-vision-codex-test-report.md
5. the top section "Targeted regression after the 2026-08-21 Codex real-DSH report" in docs/verification-debt.md
6. package.json

Record exact tested main SHA. The ACTIVE instruction commits are expected to sit on top of the implementation baseline; test the pulled latest main and do not silently switch revisions.

============================================================
2. TEST SCOPE
============================================================

This is NOT a full 428-case rerun.

Primary scope:

F-001 BLOCKER — real DSH bundled understand_image crashes on dynamic node:crypto require
F-002 HIGH — internal routing/attachment metadata visible in user bubble
F-003 HIGH — old attachment cannot be read after DSH restart
F-004 HIGH — execution-gate FIFO test failure
F-005 HIGH — provider cooldown/half-open recovery failure
F-009 MEDIUM — tool-thin/orchestration contract failures
F-010 MEDIUM — Windows Node 24 root benchmark test transform failure
NEW-R1 — verify the F-002 secrecy redesign did not silently destroy sent-image preview/history UX

Do NOT run the full Muse quality benchmark in this cycle.
Do NOT retry all five Qwen models.
Do NOT attempt to solve provider capability failures.
Do NOT spend quota forcing real 413/429 failures.

Real provider topology remains:

DSH -> image-mind -> Opencode Go plan -> Muse API -> model

Independent real gateway count = 1.

Use the known working Muse MiMo visual route/model for the minimum real-image validation. If model discovery shows a different actual working MiMo id, record it and use the discovered real id. Do not guess model ids.

============================================================
3. TARGETED REPOSITORY TESTS FIRST
============================================================

Run from repository root unless the script requires otherwise.

A. Clean install / environment

npm ci

B. Targeted image-mind tests

Run the repository's normal Vitest invocation for these files, adapting only the CLI path needed by the workspace layout; do not edit config:

packages/image-mind/tests/send-hook-routing.test.ts
packages/image-mind/tests/system-prompt-routing.test.ts
packages/image-mind/tests/attachment-ref-index.test.ts
packages/image-mind/tests/raw-route-restart.test.ts
packages/image-mind/tests/session-attachment-routing.test.ts
packages/image-mind/tests/execution-gate.test.ts
packages/image-mind/tests/provider-reliability.test.ts
packages/image-mind/tests/vision-orchestration.integration.test.ts

Also run tool-thin.test.ts if present and record the result.

C. Targeted root benchmark transform regression

Run all three root suites that previously failed collection:

tests/benchmark-score.test.ts
tests/benchmark-compare.test.ts
tests/benchmark-runner.test.ts

The important F-010 assertion is not merely "CLI works". Vitest on the original Windows Node 24 environment must successfully COLLECT and EXECUTE these suites without:

SyntaxError: Invalid or unexpected token

Record number of suites/tests collected and passed.

D. Build/package blocker gate

npm run typecheck
npm run build
npm run test:package
npm run test:built

F-001 cannot be declared PASS solely from source tests. test:built/build must pass and the built package must then be exercised through real DSH below.

============================================================
4. F-001 — REAL DSH BUILT-BUNDLE RETEST
============================================================

Use an isolated DSH profile when possible and use the official DSH plugin install mechanism. Test the newly built/current package, not a stale previous installation.

Record:

- DSH version
- Node version
- npm version
- profile name
- exact source SHA
- install/reinstall result
- whether the loaded plugin corresponds to the current build

Scenario:

1. Start real DSH web UI.
2. Confirm image-mind settings/tool are loaded.
3. Paste or attach a small controlled PNG.
4. Send an image-dependent request. An image-only send is especially valuable because it proves routing without user text.
5. Observe whether the main agent invokes understand_image.
6. Observe tool result and final response.

PASS requires:

- understand_image actually executes through the shipped DSH bundle;
- no `Dynamic require of "node:crypto" is not supported` error;
- visual evidence is returned from the real MiMo path;
- final answer is based on tool evidence, not guessing;
- no repeated duplicate tool calls caused purely by the old bundle crash.

If the bundle still throws a dynamic-require error, mark F-001 BLOCKER FAIL and stop expensive real-provider work after collecting enough evidence.

============================================================
5. F-002 — USER-VISIBLE SECRECY + HIDDEN MODEL ROUTING
============================================================

This has TWO independent acceptance conditions. Both must pass.

A. User-visible secrecy

Send:

1. one image with text, e.g. `这张图里写了什么？`
2. one image with no user text if the UI allows it
3. two images with a short comparison request

Inspect the rendered USER bubbles.

Allowed user-visible text:

- the user's own text
- neutral marker `已附加图片。`
- neutral marker `已附加 N 张图片。`

The user bubble must NOT expose any of:

- `understand_image`
- tool-routing instructions
- HTML routing comments
- `<!--`
- `[image attachment`
- `attachmentId`
- `sha256:`
- attachment JSON
- `mediaType`
- dimensions / byte metadata
- `/image-mind/raw/`
- raw internal URL

Capture concise evidence or screenshot references in the report without exposing secrets.

B. Hidden model routing still works

The secrecy fix must not make the agent blind.

Verify that an image-dependent request still results in the main DSH model calling understand_image.

For the current-session upload path, the expected tool call may omit `image` and `images`; image-mind should resolve the latest session batch using the host-side session attachment index.

When possible, inspect/trace the DSH prompt assembly and verify a model-only `tool:image-mind` system-prompt section is present while absent from the rendered user message.

PASS requires BOTH:

- no internal routing data appears in user UI;
- the main model still knows to call understand_image and successfully receives visual evidence.

If secrecy passes but routing fails, F-002 is still FAIL/HIGH.

============================================================
6. F-003 — RESTART ATTACHMENT RECOVERY
============================================================

Goal: prove attachment recovery no longer depends on embedding full metadata into conversation text.

Use a controlled image whose content can be verified after restart.

Procedure:

1. Send/upload the image in real DSH.
2. Capture its attachment id only in your local test evidence if you can obtain it from host/network/debug data. DO NOT inject it into the conversation just to make the test pass.
3. Confirm the user bubble itself contains no id/raw URL/metadata as required by F-002.
4. Restart the DSH host/process normally.
5. Resume the SAME session.
6. If the raw id is available to the test harness, request `/image-mind/raw/<id>` and record HTTP status/content-type without writing the id into the report; use `<REDACTED_ATTACHMENT_ID>`.
7. Ask the resumed conversation to inspect/read the old image again.

PASS requires:

- host restart completes;
- the durable host-side attachment metadata index restores the complete ImageAttachmentRef;
- raw route returns HTTP 200 when testable;
- resumed-session understand_image can read the persisted old image;
- the old image can be answered correctly without metadata embedded in conversation text.

If raw route is 200 but the resumed agent cannot resolve the old session image, report PARTIAL/FAIL with layer distinction.

If the attachment backend itself does not persist in the selected DSH profile, mark BLOCKED only with concrete evidence; do not invent persistence.

============================================================
7. F-004 — EXECUTION GATE
============================================================

Run the Windows Node 24 FIFO/cancellation tests.

Verify:

- with capacity occupied, queued operation 3 does not start early;
- after a slot is released, operation 3 eventually starts;
- order remains FIFO;
- the test does not rely on exactly two Promise.resolve() microtasks;
- aborted waiter does not consume/leak capacity;
- subsequent queued work can proceed.

Do not change production gate logic or test timing to force green.

PASS only if the existing current test suite passes as written.

============================================================
8. F-005 — PROVIDER RELIABILITY / CIRCUIT RECOVERY
============================================================

Run deterministic LOCAL MOCK provider reliability and circuit tests.

Verify all of:

1. Initial healthy ordering is stable.
2. Provider-level recoverable failures accumulate as expected.
3. Threshold opens the circuit.
4. Open provider is excluded during cooldown.
5. After cooldown, exactly ONE half-open recovery probe is admitted.
6. That recovery probe actually enters the bounded fallback candidate set even if other healthy providers exist.
7. Concurrent callers do not get multiple half-open probes.
8. Probe success closes the circuit and restores normal eligibility.
9. Probe failure reopens the circuit.
10. 400/401/auth/model incompatibility does not wrongly pollute provider health.

This is LOCAL MOCK / SINGLE-GATEWAY LOGIC evidence only. Do NOT report it as real independent-provider validation.

============================================================
9. F-009 — CONTRACT REGRESSION
============================================================

Run:

- tool-thin.test.ts
- vision-orchestration.integration.test.ts

Verify:

A. task-aware `maxOutputTokens` no longer causes the thin-tool structural rule to treat provider-neutral budgeting as forbidden provider internals.

B. orchestration contract explicitly expects trace telemetry rather than failing because trace exists.

For the orchestration fallback/cache scenario, record at least:

First successful fallback call:
- providerCalls
- cacheHits
- modelFallbacks

Second call using successful fallback cache:
- providerCalls
- cacheHits
- modelFallbacks

The second result may differ in trace from the first even when text/provider/model are the same. That is expected telemetry, not a reason to strip trace.

============================================================
10. F-010 — WINDOWS NODE 24 BENCHMARK TRANSFORM
============================================================

This must be tested on the same class of environment that exposed the failure: Windows + Node 24 if available.

Run the three root benchmark suites together and individually if needed.

PASS requires:

- all three files collect;
- tests execute;
- no transform `SyntaxError: Invalid or unexpected token`;
- imported `.mjs` benchmark modules are usable from Vitest.

If the same transform error remains, record the exact failing file/import edge and Node/Vitest versions. Do not modify the scripts.

============================================================
11. NEW-R1 — CONVERSATION PREVIEW / HISTORY UX REGRESSION CHECK
============================================================

This is a deliberate new check because the F-002 fix removed raw markdown URLs from conversation text.

Test:

1. Attach/send a PNG in DSH web UI.
2. Observe pending/draft preview before send.
3. After send completes, inspect the historical user message.
4. Switch away/back to the session if practical.
5. Restart DSH and reopen the session if practical.

Record separately:

- draft preview before send: PASS/FAIL
- sent-message historical thumbnail/gallery after send: PASS/FAIL/PARTIAL
- click/open full-size behavior if present
- history/session reload behavior

IMPORTANT:

A failure here is NOT permission to restore `sha256:` ids or `/image-mind/raw/` URLs into user-visible conversation text.

If sent-message preview disappears because the old preview enhancer depended on parsing raw markdown, report NEW-R1 as a regression with severity and evidence. Do not fix it.

============================================================
12. FULL STATIC LANE AFTER TARGETED CHECKS
============================================================

If the targeted blocker/high checks are not blocked by an immediate real-DSH BLOCKER, run the complete static lane:

npm ci
npm run typecheck
npm test
npm run build
npm run test:package
npm run test:built

Record command, exit code, duration, test counts, and any failures.

Do not count a piecemeal targeted pass as equivalent to the complete lane.

============================================================
13. WHAT NOT TO TEST / WHAT NOT TO CLAIM
============================================================

Do NOT run full 428-case real-provider quality regression yet.
Do NOT run a 21-case MiMo/Qwen head-to-head.
Do NOT keep probing all Qwen ids; prior evidence already showed 0/5 visual compatibility.
Do NOT force real 413 or 429.
Do NOT change provider caps to hide long-screenshot failures in this cycle.
Do NOT claim independent provider failover is validated; only one real gateway exists.
Do NOT mark previous verification-debt items PASS unless this run actually executes them.
Do NOT modify docs/verification-debt.md.

============================================================
14. REPORT FORMAT
============================================================

Create exactly:

docs/test-results/dsh-vision-codex-blocker-high-retest.md

Required structure:

# dsh-vision Codex Targeted BLOCKER/HIGH Retest

## 1. Test Identity
- Repository
- Branch
- Source Commit SHA
- Date/time
- OS
- Node
- npm
- DSH
- Test Mode: TEST ONLY
- Credential: PRESENT/MISSING only

## 2. Workspace Safety
- Dirty Before
- Dirty After before report creation
- statement: NO SOURCE MODIFICATIONS

## 3. Targeted Static Results
Table: test/command, status, duration, evidence

## 4. F-001 Real DSH Built-Bundle Retest
Status + evidence

## 5. F-002 User Secrecy / Hidden Routing
Separate UI secrecy and model-routing subresults

## 6. F-003 Restart Attachment Recovery
Raw-route/resumed-session evidence

## 7. F-004 Execution Gate
Status + exact tests

## 8. F-005 Circuit / Provider Reliability
Status + half-open single-probe evidence

## 9. F-009 Contract Tests
Status + trace summary

## 10. F-010 Windows Node 24 Benchmark Transform
Status + collected/executed suite counts

## 11. NEW-R1 Conversation Preview / History UX
Draft/sent/history/restart subresults

## 12. Full Static Lane
All six commands

## 13. Failure Table
For every failure:
- ID
- severity
- area
- reproduction
- expected
- actual
- evidence
- likely layer

Do not propose or implement code patches.

## 14. Blocked / Not Run
Concrete reasons only

## 15. Final Targeted Verdict

Use exactly one:

TARGETED GREEN — READY FOR FULL REGRESSION

or

TARGETED NOT GREEN — FIX BEFORE FULL REGRESSION

The GREEN verdict requires F-001..F-005, F-009, F-010 and the full static lane to pass. NEW-R1 is reported independently; if it reveals a serious user-facing regression, clearly say whether it should block full regression/release even if core blocker/high checks pass.

============================================================
15. COMPLETION COMMIT
============================================================

Before staging:

git status --short

Allowed final staged paths ONLY:

docs/test-results/dsh-vision-codex-blocker-high-retest.md
docs/agent-tasks/ACTIVE_CODEX_TASK.md          [deletion]
docs/test-instructions/ACTIVE_CODEX_TEST_PROMPT.txt [deletion]

Stage only those paths.

Then:

git diff --cached --name-only

If any source, test, config, package, lockfile, README, CHANGELOG, verification-debt, or unrelated file appears, unstage it. Do not commit it.

Suggested commit message:

test: record blocker-high DSH retest

Push to main only if permitted and only with the report + two ACTIVE deletions.

Record the report commit SHA.

============================================================
16. FINAL CHAT RESPONSE
============================================================

Keep the final Codex chat reply short. Return only:

Source Commit SHA: <sha>
Report Commit SHA: <sha or BLOCKED>
Targeted PASS: <n>
Targeted FAIL: <n>
Targeted PARTIAL/BLOCKED/NOT RUN: <n>
BLOCKER: <n>
HIGH: <n>
Verdict: <TARGETED GREEN... or TARGETED NOT GREEN...>
Report: docs/test-results/dsh-vision-codex-blocker-high-retest.md

No long transcript in chat. Full evidence belongs in the GitHub report.
